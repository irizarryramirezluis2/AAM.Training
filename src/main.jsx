import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../Src/api/app'
import '../Src/api/index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
